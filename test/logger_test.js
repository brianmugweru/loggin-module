const fs = require('fs');
const request = require('supertest');
const app = require('../index');
const http = require('http');
const { logOnRequest } = require('..');

describe('logOnRequest', () => {
  it('returns current timeStamp as id', (done) => {
  });

  it('formats data', (done) => {
  });
});

